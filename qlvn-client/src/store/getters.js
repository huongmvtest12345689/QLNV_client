// retrive and filter data
export default {
    userList: state => state.userList,
    files: state => state.files,
    data: state => state.data,   
    filename: state => state.filename,
    messages: state => state.messages,
    countryChange: state => state.countryChange
}